name: 🚀 Auto Deploy to Firebase

# DISABLED: GitHub Pages is the primary deployment method for newtifi.com
# Firebase deployments were failing. Use GitHub Pages instead.
on:
  workflow_dispatch: # Manual trigger only - disabled auto-deploy
  # push:
  #   branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4
      
    - name: 📦 Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
        
    - name: 📦 Install dependencies
      run: npm ci
      
    - name: 🔨 Build application
      run: npm run build
      
    - name: 🔥 Deploy to Firebase
      uses: FirebaseExtended/action-hosting-deploy@v0
      with:
        repoToken: '${{ secrets.GITHUB_TOKEN }}'
        firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT }}'
        channelId: live
        projectId: newtifi-web
        
    - name: ✅ Deployment complete
      run: |
        echo "🚀 Deployment successful!"
        echo "🌐 Site is live at: https://newtifi.com"